#pragma once
#include <Windows.h>

enum MouseKey {
	LEFT   = VK_LBUTTON,
	RIGHT  = VK_RBUTTON,
	CENTER = VK_MBUTTON
};

enum  Flame{

	KEY_CUR = 0,
	KEY_PRE,
};


const int INPUT_FLAME = 2;
const int KEY_NUM     = 3;