#pragma once

enum MouseKey {
	LEFT   = 0,
	RIGHT,
	CENTER,
	MAX_OF_NUM
};

enum  Flame{

	KEY_CUR = 0,
	KEY_PRE,
};

enum MouseState {
	START,
	END
};
const int INPUT_FLAME = 2;
const int KEY_NUM     = 256;