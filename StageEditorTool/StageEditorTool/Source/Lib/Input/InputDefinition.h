#pragma once
#include <Windows.h>

enum MOUSE_KEY {
	LEFT   = VK_LBUTTON,
	RIGHT  = VK_RBUTTON,
	CENTER = VK_MBUTTON
};

enum  FLAME{

	KEY_CUR = 0,
	KEY_PRE,
};

const int INPUT_FLAME = 2;
const int KEY_NUM     = 3;